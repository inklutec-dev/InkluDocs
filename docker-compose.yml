services:
  inkludocs:
    image: inkludocs:v4-20260918-verwaltung1
    container_name: inkludocs
    restart: unless-stopped
    # 15.09.2026 (Steve, nach dem Vorfall vom 14.09.): Logs ins System-Journal — sie ueberleben den
    # Container-Neubau beim Rollout (Verlauf: journalctl CONTAINER_NAME=<dienst> --since "..."; docker logs
    # zeigt weiterhin den laufenden Container). Zeitzone Berlin fuer Log- und Mail-Zeiten; die Datenbank
    # bleibt UTC (SQLite datetime('now')), beim Vergleich +2 h (Sommer) / +1 h (Winter) rechnen.
    logging:
      driver: journald
      options:
        tag: "{{.Name}}"
    ports:
      - "127.0.0.1:8001:8001"
    volumes:
      - inkludocs_data:/app/data
    environment:
      - TZ=Europe/Berlin
      # Log-Zeilen sofort ausgeben (01.09.2026): print ist im Container sonst gepuffert, Zeitstempel im Log stimmen nicht.
      - PYTHONUNBUFFERED=1
      # PDF_LANGBESCHREIBUNG (03.06.2026): Langbeschreibung bei PDF an/aus, Standard off. Siehe .env / backend main.py.
      - PDF_LANGBESCHREIBUNG=${PDF_LANGBESCHREIBUNG:-off}
      # Production-spezifisch
      - SECRET_KEY=${SECRET_KEY:-inkludocs-production-key-2025}
      - BASE_URL=https://inkludocs.inklutec.de
      - REGISTRATION_ENABLED=true
      - NOTIFICATION_EMAIL=${NOTIFICATION_EMAIL:-support@inklutec.de}
      # SMTP
      - SMTP_SERVER=${SMTP_SERVER}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      # v4 + Bedrock - Defaults sind Production-Werte (Fail-Safe wenn --env-file vergessen)
      - V4_VERIFY_MODE=${V4_VERIFY_MODE:-kritisch}
      - V4_VERIFY_KORREKTUR=${V4_VERIFY_KORREKTUR:-on}
      - V4_ZAEHL_PASS=${V4_ZAEHL_PASS:-on}
      - V4_DIAGRAMM_WERTE=${V4_DIAGRAMM_WERTE:-on}
      - V4_FAKTENBLATT=${V4_FAKTENBLATT:-on}
      - V4_PROMPT_CACHE=${V4_PROMPT_CACHE:-off}  # Prompt-Caching 03.09.2026: Bilddaten ans Ende, fester Anfang mit cache_control (off = Prompts wie vorher)
      # Abo-/Credit-System (31.07.2026): off = nur zaehlen, on = Kontingente durchsetzen (Review-Befund 3)
      - ABO_ENFORCEMENT=${ABO_ENFORCEMENT:-off}
      # Rollout 31.08.2026: Tageslimit ausdruecklich (wie Staging), Umwandler-Dienst, Tageslauf, Stripe
      - DAILY_IMAGE_LIMIT=${DAILY_IMAGE_LIMIT:-500}
      - KONVERTER_URL=http://inkludocs-konverter:8090
      - TAGESLAUF_TOKEN=${TAGESLAUF_TOKEN:-}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY:-}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
      - AWS_REGION=${AWS_REGION:-eu-central-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - BEDROCK_MODEL_CLASSIFY=${BEDROCK_MODEL_CLASSIFY:-eu.anthropic.claude-sonnet-4-6}
      - BEDROCK_MODEL_INVENTAR=${BEDROCK_MODEL_INVENTAR:-eu.anthropic.claude-sonnet-4-6}
      - BEDROCK_MODEL_GENERATE=${BEDROCK_MODEL_GENERATE:-eu.anthropic.claude-sonnet-4-6}
      - BEDROCK_MODEL_VALIDATE=${BEDROCK_MODEL_VALIDATE:-eu.anthropic.claude-sonnet-4-6}
      # Anbieter je Rolle (08.09.2026): Vorgabe bedrock; gemini erst nach Vertex-EU-Einrichtung und Freigabe
      - LLM_PROVIDER=${LLM_PROVIDER:-bedrock}
      - LLM_PROVIDER_CLASSIFY=${LLM_PROVIDER_CLASSIFY:-}
      - LLM_PROVIDER_INVENTAR=${LLM_PROVIDER_INVENTAR:-}
      - LLM_PROVIDER_GENERATE=${LLM_PROVIDER_GENERATE:-}
      - LLM_PROVIDER_VALIDATE=${LLM_PROVIDER_VALIDATE:-}
      - GEMINI_AUTH=${GEMINI_AUTH:-vertex}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - VERTEX_PROJECT=${VERTEX_PROJECT:-}
      - VERTEX_REGION=${VERTEX_REGION:-europe-west1}
      - VERTEX_REGION_MAP=${VERTEX_REGION_MAP:-}
      - GOOGLE_SERVICE_ACCOUNT_JSON_B64=${GOOGLE_SERVICE_ACCOUNT_JSON_B64:-}
      - GEMINI_MODEL_CLASSIFY=${GEMINI_MODEL_CLASSIFY:-gemini-3.8-flash}
      - GEMINI_MODEL_INVENTAR=${GEMINI_MODEL_INVENTAR:-gemini-3.1-pro-preview}
      - GEMINI_MODEL_GENERATE=${GEMINI_MODEL_GENERATE:-gemini-3.1-pro-preview}
      - GEMINI_MODEL_VALIDATE=${GEMINI_MODEL_VALIDATE:-gemini-3.1-pro-preview}
      # Features
      - PDFIX_ENABLED=${PDFIX_ENABLED:-true}
      - PDF_TAGGING_WEG=${PDF_TAGGING_WEG:-pdfix}   # Tagging-Weg: struktur (KI-Zuordnung, PDFix schreibt) | pdfix (23.09.2026)
      # PDFix-Lizenz (27.08.2026): aus der Env-Datei; leer = Testversion
      - PDFIX_LICENSE_USER=${PDFIX_LICENSE_USER:-}
      - DAILY_CHAT_LIMIT=${DAILY_CHAT_LIMIT:-100}   # Chat-Bremse je Konto/Tag (28.08.2026)
      - PDFIX_LICENSE_KEY=${PDFIX_LICENSE_KEY:-}
      - DEBUG_GEN_RAW=${DEBUG_GEN_RAW:-false}
      - INKLUAGENT_ENABLED=${INKLUAGENT_ENABLED:-true}
      - INKLUAGENT_BEDROCK_MODEL_TEXT=${INKLUAGENT_BEDROCK_MODEL_TEXT:-eu.anthropic.claude-sonnet-4-6}
      - INKLUAGENT_BEDROCK_MODEL_VISION=${INKLUAGENT_BEDROCK_MODEL_VISION:-eu.anthropic.claude-sonnet-4-6}
      - INKLUAGENT_INTENT_MODEL=${INKLUAGENT_INTENT_MODEL:-eu.anthropic.claude-haiku-4-5-20251001-v1:0}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - INKLUAGENT_AGENTIC=${INKLUAGENT_AGENTIC:-true}
      # Chatbot-Anbieter (10.09.2026): bedrock (Vorgabe) oder gemini; Gemini-Modelle/Denkstufe/Temperatur optional
      - INKLUAGENT_PROVIDER=${INKLUAGENT_PROVIDER:-bedrock}
      - INKLUAGENT_VERIFY=${INKLUAGENT_VERIFY:-on}
      - ABLAGE_WORD=${ABLAGE_WORD:-off}   # Word-Dateien zusaetzlich in die Ablage (Steve 11.09.: aus)
      - KOSTEN_PRO_CREDIT_EUR=${KOSTEN_PRO_CREDIT_EUR:-0.012}
      - INKLUAGENT_GEMINI_MODEL_TEXT=${INKLUAGENT_GEMINI_MODEL_TEXT:-gemini-3.1-pro-preview}
      - INKLUAGENT_GEMINI_MODEL_VISION=${INKLUAGENT_GEMINI_MODEL_VISION:-gemini-3.1-pro-preview}
      - INKLUAGENT_GEMINI_THINKING=${INKLUAGENT_GEMINI_THINKING:-}
      - INKLUAGENT_GEMINI_TEMPERATURE=${INKLUAGENT_GEMINI_TEMPERATURE:-1.0}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - default
      - inklucut_default

  # Umwandler (Rollout 31.08.2026): LibreOffice + veraPDF 1.31.163 (festgenagelt), nur im
  # Compose-Netz erreichbar, kein Port nach aussen, keine Volumes. Getestetes Staging-Image.
  inkludocs-konverter:
    image: inkludocs-konverter:v4-20260901-lo262
    container_name: inkludocs-konverter
    restart: unless-stopped
    networks:
      - default

volumes:
  inkludocs_data:

networks:
  inklucut_default:
    external: true
